[project]
name = "agno-cookcook"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests",
    "openai",
    "python-dotenv",
    "agno",
    "lancedb>=0.5.0"
]
